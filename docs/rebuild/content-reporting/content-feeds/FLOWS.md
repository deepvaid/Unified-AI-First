# Content Feeds — flows

## Flow 1 — NEW FEED (header CTA)

1. Click → modal opens, **titled "Edit Merge Feed"** (copy bug), empty:
   Name*, Day of the Week ▾, Hour of the Day ▾, one Key*+URL* row, + ADD FEED, ⓘ note.
2. SAVE disabled until valid. CANCEL verified.
3. SAVE — NOT executed (creates a record).
4. Open question: no visible way to create a *Single* feed from this modal.

## Flow 2 — edit a merge feed (row pencil)

1. Pencil on a merge feed → "Edit Merge Feed" modal pre-filled (name, schedule,
   key/URL rows e.g. key `s1` → `https://money.com/feed/v2/marop…`).
2. + ADD FEED appends a row; row ⋮ menu (delete row — unverified).
3. CANCEL verified; SAVE not executed.

## Flow 3 — edit a single feed (row pencil)

1. Pencil on a single feed → "Edit Single Feed" modal: Name*, schedule selects,
   one URL* with 👁 preview icon, same ⓘ note.
2. 👁 — not clicked (likely fetches/preview of the external feed; network side effect).
3. CANCEL verified; SAVE not executed.

## Flow 4 — folders & bulk

- Folder toggle present (panel same family as Image Library); select-all checkbox column
  present but no bulk bar was triggered — unverified.

## Unverified

- Single-feed creation path; feed-row ⋮ menu contents; 👁 URL preview; SAVE outcomes;
  bulk actions; folder drag.

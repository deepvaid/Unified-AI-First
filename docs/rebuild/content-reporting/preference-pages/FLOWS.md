# Preference Management — flows

## Flow 1 — NEW PAGE (header CTA)

1. Click → "New Page" modal: Name*, Page Type* (Manage Subscriptions default),
   Redirect* ("Default", 👁, helper text), Select Editor* radios (Drag & Drop / WYSIWYG /
   HTML Code Editor).
2. CREATE disabled until Name. CANCEL verified.
3. CREATE → chosen editor — NOT executed (creates a record; editors cross-origin).

## Flow 2 — filters

1. "All Editor Types" ▾ and "All Page Types" ▾ filter the table. Options inferred from column
   values (menus not expanded — the editor list is definitionally the 3 editor types,
   page list the 3 page types + "All").

## Flow 3 — Preview Preference Page (kebab)

- Not followed (analogous full-screen preview verified on Footer Management; assumed same
  pattern). Flagged as inferred-by-analogy.

## Flow 4 — Edit Preference Page (kebab)

- Not followed; opens the record's editor (cross-origin builder). Unverified.

## Flow 5 — Delete Preference Page Permanently (kebab)

- NOT executed (destructive, explicitly permanent). Confirmation dialog unverified.

## Unverified

- Page Type / Redirect select options (inferred); preview surface; edit editor; delete
  confirmation; whether a page referenced by a footer can be deleted.

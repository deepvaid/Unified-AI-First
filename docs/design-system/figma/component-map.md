# Marobase — Figma ⇄ code component map

File: **Marobase Design System** — https://www.figma.com/design/RJJ0qDChSdh0FwH2UG0t7t (run `marobase-2026-09`).
Code Connect is unavailable on the Pro plan, so this table plus the variables' WEB code syntax are the contract.
Rows are added as each set is built; node ids come from `build-state.json`.

Prop vocabulary shared by both sides: `size sm|md|lg` · `emphasis default|prominent` · `variant` = structure ·
`density default|compact` · `tone` (surface: neutral|error · feedback: info|success|warning|error) ·
`state default|hover|focus|disabled (+ error / selected / active)`.

## Foundations (built)

| Figma | Node | Code |
|---|---|---|
| Variables — Primitives / Color / Nav Skin / Dimensions / Typography (267) | collections `2:2`–`2:6` | `src/design-tokens/tokens.json` → `generated/variables.css`; alias layer `src/styles/mp-theme-aliases.css`; skins `sidebar-{gray,white,dark}.css`. Full map: `variable-map.md` |
| Text styles (17) | `S:…` in ledger | `text.*`, `display.*`, `component.button.typography`, Vuetify `text-body-2` / `text-caption`; `settings-form.scss` static labels |
| Effect styles (8) | `S:…` in ledger | `shadow.*` → `--elevation-raised / -overlay / -modal`, `--mp-shadow-buttonInset`, `--dv-orbit-mic-shadow` |
| Paint styles (3 gradients) | `S:…` in ledger | `--dv-grad`, `--dv-hero-grad`, `--dv-action-gradient` (`dv-tokens.css`) |
| Cover · Getting Started · Color · Typography · Spacing & Radius · Elevation, Motion & Layering · Layout & Breakpoints | frames `14:2` `15:2` `16:2`/`17:2` `18:2` `20:2` `21:2` `26:2` | documentation of the above; every swatch/bar/tile is variable- or style-bound |
| `Icon/<name>` ×210 | grid `23:7` (components `23:10`… `25:404`) | `<v-icon>name</v-icon>` via `src/plugins/lucideIcons.ts` (lucide-vue-next). 12 names resolve through Lucide renames: `bar-chart-2→chart-column`, `bar-chart-3→chart-column-big`, `file-plus-2→file-plus`, `file-question→file-question-mark`, `filter→funnel`, `filter-x→funnel-x`, `more-vertical→ellipsis-vertical`, `pie-chart→chart-pie`, `play-circle→circle-play`, `playlist-check→list-checks`, `scatter-chart→chart-scatter`, `shield-question→shield-question-mark`, `wand-2→wand-sparkles` |

## Atoms (rows filled as built)

| Figma set | Node | Variant axes / properties | Code |
|---|---|---|---|
| **Button** ✅ | set `30:62` (page `28:2`, doc `28:3`) | Style Primary·Secondary·Outline·Ghost·Danger × Size md·sm × State default·hover·disabled = 30 · props: Label (TEXT), Show leading icon / Show trailing icon (BOOLEAN), Leading icon / Trailing icon (INSTANCE_SWAP, defaults Icon/plus, Icon/chevron-down), Focus ring (BOOLEAN) · bound: fill/stroke/label/icon colours, height size/control/*, padding button/padding-inline / space/12, gap space/8, radius radius/full, Elevation/Button Inset on Primary + Danger | `v-btn` — Primary flat color=primary · Secondary variant=tonal · Outline variant=outlined · Ghost variant=text · Danger flat color=error; md default / sm size=small; proposed `MpButton { style, size, leadingIcon, trailingIcon }`. Outline border = border/subtle as in code (ticket A8 proposes border/strong) |
| **Icon Button** ✅ | set `34:153` (page `34:2`, doc `34:3`) | Style Primary·Secondary·Outline·Ghost × Size md 40·sm 32 × State default·hover·disabled = 24 · props: Icon (INSTANCE_SWAP, default Icon/more-vertical), Focus ring (BOOLEAN) · width = height = size/control/*, radius/full, style foreground on the icon strokes | `v-btn icon` (flat · tonal · outlined · text); `MpRowActionsMenu` trigger; app-bar 40×40 pills. Needs `aria-label` in code (ticket A10); retires `size="x-small"` (A4) |
| **Field** ✅ | set `35:232` (page `35:2`, doc `35:3`) | State default·focus·error·success·disabled·readonly × Size sm 32·md 40·lg 48 = 18 · props: Label / Value / Placeholder / Message (TEXT), Show label / Show message / Show placeholder (empty) / Show leading icon / Show trailing icon (select chevron) (BOOLEAN), Leading icon / Trailing icon (INSTANCE_SWAP) · bound: box height size/control/*, radius input/radius, padding space/12, gap space/8, label gap field/label-gap; resting stroke border/strong (settings-form.scss), focus 2px accent/default, error/success 2px status, disabled/readonly border/subtle | `v-text-field` / `v-select` / `v-autocomplete` / `v-combobox` with maropostDefaults (outlined · comfortable · persistent-placeholder static label); sm → density=compact, lg → density=default; readonly → `.mp-field-readonly`; success → `.mp-field-success` + `append-inner-icon="circle-check"`; Select = same field + chevron. Composite controls use `MpFormField` |
| **Checkbox** ✅ · **Radio** ✅ · **Switch** ✅ | sets `51:40` (6) · `51:63` (4) · `51:88` (4), page `36:2`, doc `36:3` | Checkbox: Checked false·true·indeterminate × Enabled — 24px `Icon/square` · `square-check` · `square-minus` (text/secondary unchecked, accent/default checked, text/disabled disabled) · Radio: `Icon/circle` · `circle-dot` · Switch: On × Enabled — 36×14 track (text/primary @38% off · accent/default @60% on) + 20px thumb (surface/bright · accent/default) with Elevation/Light/Raised · each: Label, Show label, Focus ring · 40px row, gap space/8 | `v-checkbox` · `v-radio` in `v-radio-group` · `v-switch` — glyphs come from the icon bridge (mdi-checkbox-* / mdi-radiobox-* → Lucide); maropostDefaults density comfortable, color primary |
| **Textarea** ✅ | set `44:41` (Field page) | State default·focus·error·disabled = 4 · Label / Value / Placeholder / Message (TEXT), Show label / Show message / Show placeholder (BOOLEAN) · 88px 3-row box, input/radius, space/8 × space/12, resting stroke border/strong | `v-textarea rows=3|5` with maropostDefaults |
| **Chip** ✅ | set `39:71` (page `39:2`, doc `39:3`) | Kind filter·tag·count × Size md 24·lg 32 (+ Selected for filter) = 8 · Label (TEXT), Show leading icon (BOOLEAN), Leading icon (INSTANCE_SWAP), Close (BOOLEAN) · pill, chip/height-md|lg, space/8|12 padding, filter outline border/default (= outline-variant) | `v-chip rounded=pill size=small` (24 = the app default), `v-chip-group` (filters), `.v-chip--outlined-kit`; tag = closable chip |
| **Status Chip** ✅ | set `39:228` | Tone brand·success·warning·danger·neutral × Size sm 20·md 24·lg 32 × Variant tonal·outlined = 30 · Label (TEXT), Show icon (BOOLEAN, default on), Icon (INSTANCE_SWAP) · chip/radius 8, chip/height-*, padding chip/paddingInline (space/8) all sizes, label Component/Chip 12/600, container/on-container (.v-chip--tonal-*) or 1px stroke + coloured text | `MpStatusChip {status, type, size, variant, showIcon}` — tone from the 12 domain maps; Figma defaults the icon on (a11y A2) |
| **Source Cloud Chip** ✅ | set `40:136` | Cloud commerce·marketing·analytics·contacts·service·retail × Icon only = 12 · pill (radius/full) on surface/primary + 1px border/subtle, label Component/Chip in cloud/*/text, icon same colour @90%, chip/height-md 24, padding chip/paddingInline, gap space/4 | `MpSourceCloudChip {dataSource, size, iconOnly}` (sm 20 / lg 32 documented); neto → text/primary; ticket T12 |
| **Badge** ✅ | set `40:152` | Kind count·dot·label × Tone neutral·accent·danger = 9 · Label (TEXT) · chip/height-sm 20 pill, dot space/8 | `v-badge` (99+ cap), `.sidebar-badge`, filter-tab counts, notifications bell |
| **Avatar** ✅ | set `41:53` (page `41:2`, doc `41:3`) | Size 24·32·40·56 × Type initials·image·icon = 12 · Initials (TEXT), Icon ↔ · radius/full, size bound to space/24·32·40 (56 unbound — no token) | `v-avatar size=… color=primary-container`; retires 15 ad-hoc sizes + 5 hand-rolled initials (`.avatar-fallback`, `.sc-app-row__initials`, `.pos-customer-row__avatar`) |
| **Menu Item** ✅ | set `42:47` (page `42:2`, doc `42:3`) | State default·hover·disabled × Danger = 5 · Label, Icon (+↔), Shortcut (+text) · 36 = menu/item-height, space/12, chip/radius | `MpMenuItem {title, icon, danger}` (v-list-item role=menuitem) |
| **Nav Item** ✅ | set `42:244` | Context sidebar·rail·section-rail × State default·hover·active·disabled = 12 · Label, Icon ↔, Count / Badge / Lock / External / Chevron booleans · sidebar+rail bound to **Nav Skin** (nav/text, hover, active, active-text), section-rail to Color + 2px accent active bar · 40 = size/control/md, nav/item-radius | `AppSidebar` v-list-item rows + `.rail-flyout-item` (8 styles → 1), `MpSectionRail` items |
| **Tab** ✅ | set `42:260` | State default·hover·active = 3 · Label, Show count, Count text · 44px (v-tabs density comfortable), padding space/14, Body/Label → Component/Label Strong when selected, 2px accent slider, count = 10px text/muted (accent/default selected) | `MpFilterTabs` items (v-tab + x-small transparent count chip) |
| **Segment** ✅ | set `42:285` | Mode label·icon × State default·selected·disabled = 6 · Label, Icon · 32 in 40 track, radius/full, space/12; text/muted default, selected surface/primary + text/primary + Elevation/Light/Raised | `MpSegmentedControl` items |
| **Step** ✅ | set `42:303` | State todo·active·done·jumpable = 4 · Label, Number · 20 disc (chip/height-sm), Component/Label Strong 13/600; todo text/muted · active accent/default · done accent/container + accent/default check | `MpWizardSteps` steps (aria-current=step) |
| **Tooltip** ✅ `43:9` · **Divider** ✅ `43:13` (2) · **Spinner** ✅ `43:20` (2) · **Progress Linear** ✅ `43:21` · **Skeleton** ✅ `43:26` (3) · **Kbd** ✅ `54:6` (Size sm 20 · md 24) | page `43:2`, doc `43:3` | Tooltip: Label · ink-panel/bg+fg, space/6×10, chip/radius · Divider: Orientation · border/subtle · Spinner: Size sm 20 / md 32 arc accent/default · Progress: 4px track surface/secondary + accent value · Skeleton: Shape text/block/circle surface/secondary · Kbd: Size sm (border/default, surface/primary, space/6) · md (border/subtle, surface/secondary, chip/paddingInline), Code/Kbd 11 mono text/muted, Key · Tooltip = text/primary @90% + surface/primary text · Skeleton = text/primary @8%, text bar space/12 | `v-tooltip` (top, 150ms) · `v-divider` (opacity .72) · `v-progress-circular indeterminate` · `v-progress-linear height=4` (MpUsageMeter) · `MpTableSkeleton` / `.dv-skeleton` · app-bar `<kbd>` |
| **Illustration** ✅ | set `45:77` (page `45:2`, doc `45:3`) | Name no-results·empty-orders·empty-contacts·empty-campaigns·empty-products·empty-generic·start-here·error = 8 · 160×120, body strokes icon/secondary, accent stroke accent/default | `MpIllustration {name, size}` — `src/assets/illustrations/*.svg` |

## Molecules · Patterns · AI kit · Product — see the approved plan §4–§7; rows are appended here with node ids as each set lands.
